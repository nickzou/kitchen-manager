# PostgreSQL
POSTGRES_PASSWORD=changeme
POSTGRES_DB=kitchen_manager

# App
PORT=3000
BETTER_AUTH_SECRET=generate-a-random-secret-here

# Only needed for local dev (outside Docker)
# DATABASE_URL=postgresql://postgres:changeme@localhost:5432/kitchen_manager

# Email (SMTP)
SMTP_HOST=smtp.ethereal.email
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_SECURE=false
SMTP_FROM=
APP_URL=http://localhost:3000
